name: Deploy OriSharin Backend

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy & Restart on VPS
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            cd /home/orisharin/apps/backend
            
            # Pull latest code
            git pull origin main || (git fetch && git reset --hard origin/main && git clean -fd)

            # Create .env from GitHub Secrets
            cat > .env << 'EOF'
            NODE_ENV=production
            PORT=3000
            DATABASE_URI=${{ secrets.DATABASE_URI }}
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            CLOUDINARY_CLOUD_NAME=${{ secrets.CLOUDINARY_CLOUD_NAME }}
            CLOUDINARY_API_KEY=${{ secrets.CLOUDINARY_API_KEY }}
            CLOUDINARY_API_SECRET=${{ secrets.CLOUDINARY_API_SECRET }}
            TRANSLATE_PROVIDER=libre
            TRANSLATE_URL=https://libretranslate.de
            TRANSLATE_API_KEY=${{ secrets.TRANSLATE_API_KEY }}
            EOF

            # Install & build
            npm ci --production
            npm run build

            # Restart PM2 (handles both dist/main.js and dist/src/main.js cases)
            pm2 restart orisharin-api || pm2 start dist/src/main.js --name "orisharin-api"
            pm2 save
            
            echo "Backend deployed successfully!"
